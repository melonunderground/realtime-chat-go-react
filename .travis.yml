language: node_js
node_js:
  - stable
install:
  - cd frontend
  - npm install

script:
  - npm test
  - npm run test -- --coverage
after_script:
  - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls
